<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
    
  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

<!-- Такой способ указывать пути очень не желателен, т.к. пути прописаны жёстко -->
    <p> 
      <h3>It is not TRUE!</h3>
      <a href="/users/sign_in">Sign in (!)</a> | 
      <a href="/users/sign_out", data-method="delete" >Sign out (!)</a> 
    </p> 
    <br />
<!-- А вот такуой способ указывать пути желателен, т.к. пути прописаны переменными из "rake routes" (далее, ra-ro) -->   
    <p>
      <h3>It is TRUE !!!</h3>  
      <%= link_to 'Sign In', new_user_session_path %> |
      <!-- new_user_session_path содержит путь: "/users/sign_in".  И если он поменяется, то в ссылке путь поменяется автоматически -->
      <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
      <!-- new_user_session_path содержит путь: "/users/sign_in".  И если он поменяется, то в ссылке путь поменяется автоматически -->
    </p>
    
<!-- Аналогично сделаем остальные ссылки (через перемнные в ra-ro) -->
    <p>
      <%= link_to 'Home', root_path %> |
      <%= link_to 'Articles', articles_path %> | 
      <%= link_to 'New', new_article_path %> |
      <%= link_to 'Terms', terms_path %> |
      <%= link_to 'Contacts', new_contacts_path %> |
      <%= link_to 'About', about_path %>     
    </p> 

<!-- Сдесь сднлаем блок вывода Sign_IN и Sign_OUT в зависимости от того, залогинился пользователь или не залогинился. Дополнительно будем прописывать ЛОГИН, под которым проведена регистрация двнного пользоватенля -->    
    <h3>В зависимости от состояния РЕГИСТРАЦИИ:</h3>
    <p> 
      <% if user_signed_in? %>
        <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to 'Sign In', new_user_session_path %> 
      <% end %>
    </p>

    <%= yield %>

  </body>
</html>
